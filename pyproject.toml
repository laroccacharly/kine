[project]
name = "kine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "laroccacharly", email = "larocca.charly@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.4",
]

[project.scripts]
kine = "kine:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
